<%= form_for(@cliente_potencial) do |f| %>
  <% if @cliente_potencial.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cliente_potencial.errors.count, "error") %> prohibited this cliente_potencial from being saved:</h2>

      <ul>
      <% @cliente_potencial.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br>
    <%= f.text_field :nome %>
  </div>
  <div class="field">
    <%= f.label :descricao %><br>
    <%= f.text_area :descricao %>
  </div>
  <div class="field">
    <%= f.label :website %><br>
    <%= f.text_field :website %>
  </div>
  <div class="field">
    <%= f.label :observacao %><br>
    <%= f.text_area :observacao %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
